{ ... }:
{
  config = {
    systems = [
      "aarch64-darwin"  # unnamedhost00
      "i686-linux"      # old Vaio I need to setup
      "x86_64-darwin"   # old macbook pro I need to restore
      "x86_64-linux"    # q3dm10
    ];
  };
}
